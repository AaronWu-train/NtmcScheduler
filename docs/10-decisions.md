# 決策紀錄

依日期由舊到新排列；同日依題目發生順序。後列決策取代先列衝突項。現行行為以 `01`–`09` 與本檔最新決策為準。

## 2026-08-07：月班表成為 solver 資料邊界

- CSV／Excel 的 domain 名稱是「月班表」，C# 使用 `MonthlySchedule` 與 `EmployeeMonthlySchedule`。
- M/T 共用 `ScheduleInput(PreviousMonth, DemandMonth, RestIntervals)`，移除舊的員工、固定指派、七天歷史、班組與額度平行輸入。
- `DemandMonth` 是本月人員唯一來源，不建員工主檔。
- Excel 可使用相同欄位；本版 CLI 只實作 CSV。

## 2026-08-07：歷史與新進人員

- 歷史提供完整上月，不只是前七天。
- 本月有、上月沒有者必須是本月到職；到職日早於本月卻無歷史為無效輸入。
- 新進者在到職前的區間六日數與國定假日數，分別視為已計 R 與 R1。
- 月中到職不參與整月公平性，但到職後適用每日規則與個人月目標。

## 2026-08-07：八週區間

- 區間為連續 56 日，R 額度由 16 個六日推導，R1 額度為區間國定假日數。
- 六日不會同時列為國定假日。
- 月中切換 A/B 時，Opening 屬 A，Closing 屬 B。
- 本月 R 與 R1 軟目標由到職後本月六日／國定假日推導，不再手動輸入。

## 2026-08-07：跨月規則

- 兩個 solver 都固定建模到月底後七天，但不輸出延伸日。
- M 的夜–休–早／午計算所有與目標月相交的三日視窗。
- M 同班別區塊只看目標月、只比時段不比車站，並在月底結算。
- T 夜轉早從上月實際班表找最後夜班；沒有實際夜班就不產生跨月不足量。
- T 下月班別依早 → 午 → 夜 → 早輪轉。

## 2026-08-07：原始碼與 CLI

- 不建立 rule class、catalog、definition、encoder、DI 或外部規則設定。
- M/T 各使用 Main／Input／Rules 三個 partial 檔，建模邏輯不共用。
- C# 不放 Rule ID 或 J1–J5；Objective 使用 `Priority + 有意義名稱`。
- CLI 只有四個互動輸入，不加 flags、JSON、DI 或額外套件。
- 本版 M/T 都不做無解原因分析。

## 2026-08-07：既有人員到職日期可留空

- 空白到職日期代表該員工在目標月以前已到職，不免除上月班表要求。
- 沒有上月歷史者必須填寫落在本月內的到職日期，才能視為本月新進。
- 同一員工在上月與本月的到職日期必須同為空白或為相同日期。

## 2026-08-07：日期文字統一格式

- 所有使用者可見、匯入與匯出的完整日期使用 `yyyy-MM-dd`，月份使用 `yyyy-MM`，月日使用 `MM-dd`。
- 日期與月份必須補零並使用半形連字號，不接受斜線或依地區文化變動的格式；時間使用 `HH:mm`。

## 2026-08-07：M 午班顯示為小班

- M 的 Afternoon 班在使用者可見輸出統一顯示為「小」，例如 `1小`；T 的 Afternoon 班仍顯示為「午」。
- M 月班表輸入接受完整格式 `LB01小`、簡寫 `1小`，並相容既有的 `LB01午`、`1午`；讀入後統一輸出為 `1小` 等站號簡寫。

## 2026-08-07：月班表 R/R1 統計欄位

- 原 `上月底累計R/R1` 更名為 `月初區間累計R/R1`；原 `月底累計R/R1` 更名為 `月底區間累計R/R1`。
- 日格後新增 `當月R/R1`，由 1–31 日日格自動計算。歷史與候選列必須提供且核對一致；需求列的當月與月底統計留空。

## 2026-08-07：R休與 M 班別公平

- R休適用 M/T，是與 R1 相同的實際休假狀態，但不計入本月或 56 日的 R/R1 數量，亦不重置連續七日的一般 R 規則。
- 每人本月 R休數原訂為精確硬限制；此點已由「2026-08-10：R休改為可不使用完」取代。R休仍只能使用該人目標月標記 R* 的日期，不追蹤取得方式、餘額或跨月額度。
- M 的休假、跨站支援與班別數公平一律在所屬三站群組內比較。班別公平對早／午／夜分別使用 `n × 平方和 - 總和²`，權重為 1:1:2，置於 J5。

## 2026-08-10：非常態班型表

- 非常態班型使用獨立 `班型,時間,代碼` CSV 維護，CLI 讀成 `NonStandardShiftTable`；solver 介面只接受 typed snapshot，不讀 CSV。
- 非常態班型的代碼與時間必填、班型名稱可空白。非空白名稱與代碼共同作為月班表日格的唯一查找鍵。
- 查到的名稱或代碼轉成 X；候選輸出正規化為 `X[HH:mm-HH:mm]`。`早`、`午`、`夜`仍優先表示 T 正常班，同列代碼可表示 X。

## 2026-08-10：歷史班表指定休假格式

- 歷史班表輸入的 `R*`、`R1*`、`R休*` 分別表示已決定的 `R*[R]`、`R*[R1]`、`R*[R休]`。
- 本月需求的 `R*` 仍是尚待 Solver 決定為 R、R1 或 R休的指定休假；`R1*` 與 `R休*` 只接受於歷史班表。
- 候選班表輸出一律使用 `R*[R]`、`R*[R1]`、`R*[R休]`，不輸出尾綴星號格式。

## 2026-08-10：T 歷史混班與每月 R 基準

- T 上月歷史日格記錄逐日實際班別，同一人同月可混用早、午、夜。
- 每人的本月 R 軟目標是到職後、目標月內實際週六與週日的總天數，不得固定寫成 8；R1 軟目標同理由到職後的國定假日數推導。

## 2026-08-10：T 月班別改為軟規則

- 原 TH01 固定月班別硬限制改為 TS06 月班別不一致（`NonMonthlyShift`）軟規則；每個模型日期排入非該日期月班別的正常工作格計 +1，X 不計。
- 月班別不一致（`NonMonthlyShift`）納入 J2 班組人力品質（`StaffingQuality`），權重 9；目標月以該列 `T月班別` 為基準，月底後七天以早 → 午 → 夜 → 早輪轉後的下月班別為基準。
- 固定跨班正常工作是有效輸入。跨班人員計入實際工作班別的出勤、專業與能力，休假公平仍依人員所屬月班別比較。

## 2026-08-10：R休改為可不使用完

- `當月指定R休` 由精確需求數改為可使用上限；實際 R休 不得超過此值，且仍只能排在目標月的 R*。
- 未使用 R休 數量改列 J1 軟違反量「未使用 R休 額度」（`UnusedLeaveRest`），權重 1；既有「指定休假」（`RequestedRest`）權重改為 3，使相對權重為 1/3。
- 上限可高於可決定的 R* 數而不構成 `InvalidInput`；負數或低於已固定 `R*[R休]` 數仍為無效輸入。

## 2026-08-10：M 班位需求改為最低人數

- M 的早、午及指定車站夜班需求是最低人數；內部人員可使同站同班超過需求，不視為違規。
- 需求為 0 的站班組合仍禁止排人；外派只用於原本允許的站班，且維持既有最小化目標。

## 2026-08-10：候選檔自動避開既有檔案

- CLI 不再詢問是否覆寫候選檔，也不覆寫既有的候選主檔或外派檔。
- 若本次預定使用的編號已有檔案，整批候選改用下一段連續可用編號；M 的主檔與外派檔維持相同編號。

## 2026-08-10：T 每班至少兩位高能力人員

- 高能力人員不足（`Ability`）不再衡量正常出勤者的平均能力是否至少為 3。
- 每個目標月日期、每個實際班別希望至少有 2 位能力 4–5 的正常出勤者；至少 2 位時違反量為 0、只有 1 位時為 1、完全沒有時為 10。跨班人員按實際工作班別計入。
- 高能力人員不足（`Ability`）維持在 J2 班組人力品質（`StaffingQuality`），權重維持 1。

## 2026-08-10：工作區段與同班別區塊長度懲罰

- 共用長度懲罰 `D(L)` 改為：長度 1、2、3、4、5、6 以上依序為 4、3、1、0、2、5。
- 此懲罰同時適用 M/T 的連續工作區段，以及 M 的同班別區塊；群組內權重不變。

## 2026-08-10：每月休假分布取消免罰偏差

- M/T 的每月一般 R 與 R1 分布改以「實際數與目標數的絕對差平方」計分。
- 偏離 0、1、2、3 日的違反量依序為 0、1、4、9；原本偏離 1 日不懲罰的容許帶取消。群組內權重不變。

## 2026-08-10：M 合併品質目標與工作區段班型一致性

- M 原 J2、J3、J4 的全部項目合併至 J4 一起最佳化；非偏好輪轉也由 J5 移至 J4。M 的字典序改為 J1、J4、J5，J5 只保留公平性。
- J4 權重依 2026-09 M 候選的原始違反量尺度調整：外援 24、每月一般 R 64、每月 R1 8、非所屬站 2、連續工作區段 3、工作區段混合班型 15、夜休早 20、夜休午 16、未休假直接換班 7、非偏好輪轉 7。
- MS03 不再計算跨休假延續的同班別區塊長度，也不再使用 `D(L)`。它沿用 CS04 的實際工作區段與跨月邊界；每個在目標月內結束的區段若包含超過一種正常班型，違反量 +1。X 屬於工作區段但不形成班型。
- 以 2026-09 M 候選估算，CS04 與新 MS03 的加權值約為 1887 與 1830，使兩者影響接近且 CS04 稍高。

## 2026-08-10：調整 M 綜合排班品質權重

- M 的 J4 權重調整為：每月一般 R 24、每月 R1 12、連續工作區段 4、夜休早 30、夜休午 20。
- 其餘 J4 權重不變。

## 2026-08-10：降低工作區段長度懲罰

- M/T 共用的 `D(L)` 改為：長度 1、2、3、4、5 依序為 4、2、0、0、1；長度 6 以上為 `2(L-4)`。
- 此版本較接近原始懲罰，用於降低 T 的 J4 求解難度；群組內權重不變。

## 2026-08-11：外援人力免罰額度

- M 的目標月外援總人次在 70 人次以內不計違反量；超過 70 的部分按人次計算，違反量為 `max(0, 外援總人次 - 70)`。
- `ExternalStaffing` 仍位於 J4，權重維持 24。

## 2026-08-11：非所屬站指派移至 J5

- M 的非所屬站指派（`NonHomeStation`）由 J4 移至 J5；違反量定義與權重 2 不變。

## 2026-08-11：降低兩日工作區段懲罰

- M/T 共用的 `D(L)` 將 `D(2)` 由 2 改為 1；其餘長度的懲罰不變。

## 2026-08-11：M/T 的 R1 改採八週累積餘額

- M/T 不再使用每月獨立的 R1 平方偏差，改按每人、每個 56 日區間截至月底的累積餘額計分；既有 CH04 八週精確額度硬規則不變。
- 令累積餘額為「實際／折抵 R1 減累積國定假日目標」。M 的餘額 0 或 -1 不罰；M 的餘額為正時按超額平方計分，低於 -1 時按超過 1 日的欠額平方計分。T 的正負餘額皆按完整差額平方計分。
- 此違反量命名為 `SpecialRestBalance`；M 留在 J4、權重 12，T 留在 J3、權重 1。T 的 J3 名稱同步由 `MonthlyRestDistribution` 改為 `RestDistribution`（休假分布）。

## 2026-08-11：M 五日工作區段不懲罰

- M 的連續工作區段長度懲罰將 `D(5)` 由 1 改為 0；其餘長度與 J4 權重不變。
- T 的五日工作區段懲罰維持 1，因此 M/T 不再完全共用同一個長度懲罰函數。

## 2026-08-11：M 八週萬年班表作為 CP-SAT hint

- M 可匯入 56 日萬年班表；模板第 1 日固定對應每個 16R 八週區間的第一天。每人以月班表中的模板代號人工對應，本月留白時承接上月代號。
- 模板只在 hard-only 初始搜尋前以 `CpModel.AddHint` 加入；固定日格與 R* 不套 hint，並以單 worker 使用 `repair_hint` 容許與實際上月歷史不相容，避開 OR-Tools 9.15 多 worker repair hint 的原生當機。取得第一份可行解後清除模板 hint，改以可行 incumbent hint 及原本 worker 數進行既有字典序最佳化。
- 不新增模板偏離違反量，不要求候選遵循模板，也不由 solver 自動分配模板代號。舊月班表 CSV 沒有模板欄仍可讀取。

## 2026-08-11：LB09 開放外派但首人即罰

- LB09 早、午班可由外派人力補足；夜班需求仍為零，不建立外派班位。
- `ExternalStaffing` 改為 `max(0, LB02/LB04/LB11 外派總人次 - 70) + LB09 外派總人次`，仍位於 J4、權重 24。

## 2026-08-11：M 萬年班表允許空白日

- 56 日萬年班表的每日欄位可留白；空白只表示該員工該日不提供 solution hint，不代表休假、工作或任何限制。
- 非空白格仍只接受一般 `R` 或合法 M 站班，並沿用既有站群與正需求班位驗證。

## 2026-08-11：M 班別次數公平改用最大差

- M 的早／午／夜班次數公平改為加總各三站群組內最多者與最少者的班數差，不再使用隨群組人數放大的變異數整數等價量。
- J5 的早／午／夜權重維持 1:1:2；比較範圍仍只包含目標月第一日已在職人員，X 與延伸日仍不計。

## 2026-08-11：M 合併 J4 與 J5

- M 先最佳化 J1，再以 `1000×J4+J5` 作為單一目標最佳化；不再分別證明 J4、J5 最優。
- J5 權重改為：非所屬站 1、平日休假公平 6、假日休假公平 12、跨站支援公平 9、早／午／夜班公平 3／3／6。J4 內部權重不變。
- 此倍率是加權取捨而非嚴格字典序；J5 改善超過 1000 分時可接受 J4 增加 1 分。

## 2026-08-11：M 在 TLE 時保留同分替代候選

- M 候選維持至少 10% 的可比較格差異，並須維持相同的已達目標分數；找到幾份就輸出幾份，最多 3 份。
- M 在總時限大於 30 秒時固定預留最後 30 秒搜尋替代候選；不延長總時限。30 秒以下的短時限執行不另切候選時間。當前目標只取得 feasible incumbent 時，先固定該 incumbent 分數再搜尋同分候選。
- T 的候選差異規則與求解流程不變。

## 2026-08-12：M 排班品質與公平性改採直接權重

- M 先最佳化 J1，再直接加權合併 J4 與 J5；移除 `1000×J4` 倍率。
- J4 權重改為：外援 24、每月一般 R 24、八週 R1 餘額 12、連續工作區段 4、工作區段混合班型 3、夜休早 30、夜休午 20、未休假直接換班 2、非偏好輪轉 1。
- J5 相對權重改為：非所屬站 0.1、平日休假公平 10、假日休假公平 20、跨站支援公平 1、早／午／夜班公平 2／2／30。跨站支援公平只作同品質方案的低權重取捨；夜班不公平與夜休早同為最高權重 30。
- CP-SAT 與 Objective 輸出使用上述相對權重的 10 倍整數值，以精確表示 0.1；等比例縮放不改變最佳化取捨。
- LB02／LB04／LB11 共用的外援免罰額度由 70 降為 60 人次；LB09 仍從首人起計分。前 60 人次仍不計分，因此此調整只讓外援懲罰提早開始。

## 2026-08-12：M 在 TLE 時允許較差的第二候選

- M 若已有合法 incumbent 但目標未證明最佳，備援候選不再強制維持當前目標同分；仍須符合全部硬限制，且與每份既有候選至少有 10% 可比較格差異。
- M 優先在 TLE 時輸出至少 2 份候選。總時限大於 60 秒時預留 30 秒搜尋候選，否則預留總時限的一半；若模型沒有符合差異門檻的第二解或預留時間不足，仍保留實際找到的候選。
- 進入替代候選搜尋後清除品質最佳化方向，改為只搜尋符合既有最佳值等式（若有）、硬限制與差異門檻的可行解，避免把預留時間再次耗在證明品質目標。
- 所有目標已證明最佳時，替代候選仍維持相同完整目標分數。T 的候選流程不變。

## 2026-08-12：M 公平性改用正規化絕對偏差

- M 的平日休假、假日休假、跨站支援與早／午／夜班次數公平，不再只比較群組內最多者與最少者；改為計入群組內每一人的絕對偏差。
- 每群有 `n` 人、個人次數 `cᵢ`、群組總次數 `T` 時，輸出違反量為 `floor(10 × Σ|n × cᵢ − T| ÷ n)`，亦即每人偏離群組平均的絕對差合計，以 0.1 為單位表示。除以群組人數可避免不同人數的群組產生不必要的尺度差異。
- 六項公平性輸出權重維持相對值 10／20／1／2／2／30，不再額外乘以 10；其違反量已放大 10 倍。其他 M 項目的整數權重維持原來的 10 倍表示，因此所有加權分數仍在同一尺度。
- 此決策不改變 T 公平性公式，也不處理多餘夜班或缺班配置。

## 2026-08-12：M 夜班每站每日不得多人

- M 的每個日期、每個車站至多安排 1 位夜班人員；`LB01`、`LB06`、`LB08`、`LB12` 的夜班需求為 1，因此必須恰好 1 人，其餘車站夜班仍為 0 人。
- 早、午班維持最低人數規則，仍可安排超過需求的內部人員。本限制不適用於 T。
- 若需求月固定格指定兩人到同站同日夜班，因硬限制互相衝突而回傳 `Infeasible`。

## 2026-08-12：候選差異降為 5%，M TLE 品質放寬上限 20%

- M/T 候選間的最低差異由可比較格數的 10% 降為 5%，仍須與每份既有候選分別達到門檻。
- M 由 feasible incumbent 進入 TLE 備援搜尋時，每個具名目標分數最多只能比第一份高 20%；以 `候選分數 × 5 <= 第一份分數 × 6` 的整數限制精確判定。第一份為 0 的目標不得變差。
- 第一份尚無完整具名目標分數，或找不到同時符合硬限制、5% 差異與 20% 品質上限的第二份 M 候選時，只輸出第一份，不以無法判定或明顯較差的方案湊數。
- T 維持既有行為：只在 J1–J5 全部證明最佳並固定同分後搜尋替代候選，不允許候選品質變差。

## 2026-08-12：重調 M 綜合品質權重與外援額度

- LB02／LB04／LB11 共用外援免罰額度由 60 調為 70 人次；LB09 改為前 4 人次免罰，超過後才按人次計算。外援輸出權重由 240 降為 100。
- 夜休早／夜休午輸出權重提高為 400／300；平日／假日休假公平降為 5／5，夜班公平降為 10；早／午班公平維持 2／2。
- 跨站支援公平與非偏好輪轉權重改為 0，仍保留違反量輸出供人工檢視；非所屬站指派維持 1，其餘權重不變。公平違反量本身以 0.1 為單位，較大的數值尺度由較低權重校正。

## 2026-08-12：降低 M 換班與跨站指派影響

- 未休假直接換班的輸出權重由 20 降為 5；非所屬站指派的輸出權重由 1 降為 0。
- 兩項違反量仍保留在候選輸出供人工檢視；權重 0 代表非所屬站指派不再影響方案選擇，但合法三站群組的硬限制不變。

## 2026-08-12：取消 M 兩日工作區段罰分

- M 的連續工作區段長度罰分 `D(2)` 由 1 改為 0；其餘 M 長度罰分不變。
- T 的兩日工作區段罰分仍為 1，不受本次調整影響。

## 2026-08-12：CLI 顯示求解耗時

- M/T CLI 結果摘要顯示 solver 呼叫的實際經過秒數，取小數一位。
- 耗時不包含互動輸入、CSV 讀取與候選檔寫入，方便直接比較不同時限、worker 與權重設定的求解表現。

## 2026-08-12：M 暫停零權重規則並改用短時多 seed

- 非偏好輪轉、非所屬站指派與跨站支援公平不再以權重 0 保留，而是暫時完全移出 M 模型與候選目標明細；合法三站群組硬限制不變。
- M CLI 同時執行 seed 0 與 seed 1，兩次各限 3 分鐘、各用 4 workers；總 CPU workers 維持 8，wall time 目標約 3 分鐘。
- CLI 比較每次第一候選的具名目標，依 Priority 字典序採用較佳一次的完整候選集合；無候選或尚無完整具名分數者排在有完整分數的結果之後。T 與直接呼叫 solver library 的預設值不變。

## 2026-08-12：M portfolio 提供單一 CLI 開關

- M CLI 新增 `--m-search workers=N,seeds=N,seconds=N`；依序控制每個 seed 的 worker 數、並行 seed 數，以及每次搜尋的超時秒數。
- 省略開關時維持 workers 4、seeds 2、seconds 180；seed 使用從 0 開始的連續整數。最大同時使用量約為 `workers × seeds`，由使用者自行配置。
- 三個欄位皆為必填正整數，未知、缺漏、重複欄位或把此 M 專用開關用在 T 輸入時，CLI 回報格式錯誤而不啟動求解。

## 2026-08-12：M 早午班每站每日最多兩人

- M 的每個日期、每個車站，早班與午班的內部人員及外派人數合計各至多 2 人，並維持至少滿足 1 人需求。
- 夜班維持每站每日最多 1 人；有需求時恰好 1 人，無需求時不得排人。本限制不適用於 T。
- 若固定格造成同站同日早班或午班超過 2 人，因與硬限制衝突而回傳 `Infeasible`。

## 2026-08-12：M 班別公平改為個人目標

- 早班與小班不再各自比較站群平均；改為每人當月 `max(0, |早班數 − 小班數| − 4)` 的合計，權重 2。
- 夜班不再比較站群平均；每人 0、1、2、3、4 天的罰分依序為 10、5、1、0、0，5 天起為 `4 × (夜班數 − 4)`，權重 10。
- M 刪除平日休假公平，只保留假日休假公平，權重 5。夜班目標權重高於其他公平性項目；T 不變。

## 2026-08-12：M 早午班人數上限依車站區分

- `LB01`、`LB06`、`LB07`、`LB12` 的早班與午班可安排多人，不設人數上限。
- 其餘車站的早班與午班，內部人員與外派人數合計每站每日各至多 1 人。此決策取代同日「全站早午班至多兩人」的決策。
- 夜班維持所有車站至多 1 人，T 不變。

## 2026-08-12：M 零權重函數保留且候選先搜尋同分

- 非偏好輪轉、非所屬站指派與跨站支援公平的計算函數保留在 M solver；權重為 0 時，`BuildObjectiveGroups` 不呼叫它們，因此不把對應變數與限制加入 CP-SAT 模型。此決策取代「完全移出 M 模型」中刪除原始碼的做法。
- M 替代候選先像 T 一樣，固定所有具名目標與第一份完全同分後搜尋。第一次同分搜尋保留一半候選時間；若仍沒有第二份，才允許每個具名目標最多變差 20%。若已找到同分第二份，後續候選維持同分。
- 個人夜班目標 `NightShiftTarget` 權重確定為 50，取代先前記錄的 10；罰分函數不變。

## 2026-08-12：M/T portfolio 共用總時限參數與候選餘時

- M 取消固定預留候選時間，改與 T 相同：初始可行解與所有目標最佳化先共用每個 seed 的整體總時限，只以完成後餘時搜尋候選。M 的同分與 20% fallback 順序不變，但只在實際進入候選搜尋後分配當時餘時。
- CLI 統一改用 `--search workers=N,seeds=N,seconds=N`，M/T 都支援每 seed worker 數、並行 seed 數與每 seed 總時限。並行使用量約為 `workers × seeds`。
- 省略開關時，M 維持 4 workers、2 seeds、180 秒；T 維持 library 預設 8 workers、1 seed、300 秒。兩者都以各 seed 第一候選的 Priority 字典序分數選取較佳的整批結果。

## 2026-08-12：合併數學模型報告

- 刪除已過時的 `tex/main.tex`，只保留 `tex/main2.tex` 作為 D-21 指定的唯一數學模型真相來源，避免兩份公式與參數再次漂移。
- 合併後的報告以中文論文格式整理問題背景、輸入邊界、M/T 變數、硬限制、軟性目標、權重、字典序求解、萬年班表提示、portfolio 與差異候選。
- 報告中的 M 綜合目標直接列出實作整數係數；`TimeLimit` 候選分數明確標示為 incumbent 量測，不視為所有 Priority 的最佳性證明。

## 2026-08-13：T 恢復單 seed，M 維持已 commit portfolio

- T CLI 恢復為單一 seed 0，不接受 `seeds>1`；省略開關時為 8 workers、300 秒。
- M 不改變已 commit 的 portfolio：預設 4 workers、2 seeds、每 seed 300 秒，比較各 seed 第一候選的 Priority 字典序分數後，只輸出較佳 seed 的整批候選。

## 2026-08-13：M/T 假日休假公平改為平均值免罰區間

- M 仍依三站群組、T 仍依月班別分組；兩者的假日休假公平改用相同違反量。
- 每組以「組內假日實際休假總數 ÷ 組內人數」得到精確平均，不先四捨五入。每人的假日休假數落在平均正負 1.5 天內不罰；超出後，以回到最近免罰整數範圍所需的天數線性計罰。
- 例如平均為 4.5 天時，3、4、5、6 天皆為 0；2 或 7 天為 1，1 或 8 天為 2。M/T 原有比較群組、納入人員、假日與實際休假定義及權重不變；T 平日休假公平也不變。

## 2026-08-13：員工主檔改為實體刪除並允許同 ID 回任

- 員工主檔不再使用封存；刪除會移除目前 `Employee` 紀錄，並在同一 transaction 寫入含刪除前完整內容的 `EmployeeDeleted` AuditLog。
- Demand 與班表保存獨立人員快照，員工主檔刪除不得修改既有月份。AuditLog 與既有月份快照共同保留歷史，不另建重複的已刪除員工資料表。
- 刪除後釋放同單位內的員工 ID；同一人離開後回任，可用相同 ID 建立新的 `Employee` 紀錄與新的資料庫 ID。CSV 亦將該 ID 視為新增人員。

## 2026-08-13：Core 更名為 Contracts，Infrastructure 保留原名

- `NtmcScheduler.Core` 更名為 `NtmcScheduler.Contracts`，明確表示其只承載 provider-neutral DTO、command、enum、例外與 application service 介面，不是 ASP.NET Core API 或可獨立執行的後端。
- `NtmcScheduler.Infrastructure` 保留原名，承載 application service 實作、EF Core／Identity、CSV、AuditLog 與背景求解工作。
- 本次只調整專案、namespace 與文件名稱，不改變資料庫 schema、migration ID、服務契約或排班規則。

## 2026-08-13：登入、工作區授權與資安邊界

- 第一版改用 ASP.NET Core Identity 本機帳號，取代 D-04 的無登入設計；不開放註冊、忘記密碼或郵件重設。
- 所有登入者可檢視 M/T；Administrator 管理帳號，M/T 編輯權分開授予。任一 Editor 可修改共同設定。
- 密碼至少 14 字元、五次失敗鎖定 15 分鐘、Cookie 30 分鐘閒置到期且不提供記住登入。本階段不做 MFA。
- S 仍不在第一階段範圍；不建立沒有業務規格的空殼頁面或 solver。

## 2026-08-13：密碼改用一般強度規則

- 開發階段密碼至少 8 字元、至少 2 種不同字元且包含數字；不強制大小寫英文字母或非英數符號。本決策取代同日「至少 14 字元」的密碼規則。
- 此為開發階段的暫行設定，正式上線前必須依部署環境的資安要求重新檢視並提高密碼政策。
- 首次登入強制修改密碼、五次失敗鎖定 15 分鐘及登入限流等既有安全措施不變。

## 2026-08-13：Demand、班表多版本與採用指標

- 每單位每月份一份自動保存的 Demand；建立時從員工主檔複製快照，後續主檔修改不回寫舊月份。
- 每次求解候選保存成獨立且可編輯的 `ScheduleVersion`，取代 D-20 的單一目前班表。
- 同單位同月份最多一份 `AdoptedSchedule`（畫面顯示 `★`）。紅色 P0／Coverage 違規不得採用；黃色軟規則警示不阻擋。
- 班表刪除為軟刪除；採用版須先改選其他版本。所有版本內容與 AuditLog 永久保留。
- 上月 `★` 自動成為 previous schedule；若不存在，求解前必須上傳完整 previous schedule。

## 2026-08-13：共同設定版本與 Log

- 八週區間、國定假日與非常態班型每次儲存建立不可變 `ConfigurationRevision`；既有 Demand、Run 與班表凍結在原設定版本。
- AuditLog 以 append-only 資料表永久保存登入、權限、匯入、求解、班表修改、採用、封存與下載；禁止記錄密碼、Cookie、token 或連線字串。
- 系統結構化 Log 交由正式環境 journald／container logging driver 按日輪替並保留一年。

## 2026-08-14：共同設定支援 CSV 載入草稿

- 共同設定頁的八週區間與非常態班型各自提供 CSV 範本下載及上傳；格式沿用 CLI 的既有固定表頭。
- 上傳時先完整解析並取代該區塊的頁面草稿，不立即寫入資料庫；使用者仍須按「儲存設定」，才會把兩區塊目前內容一起建立為不可變版本。
- CSV 維持 UTF-8（可含 BOM）及 5 MB 上限；解析失敗時不取代目前頁面草稿，完整共同設定規則仍在建立版本時驗證。
- 前端不顯示設定版本；八週 16R 區間與非常態班型各有自己的「儲存設定」按鈕。儲存其中一區時，另一區沿用最近一次已儲存內容，未儲存的頁面編輯不會一併寫入。

## 2026-08-14：排班月份改用既有月份下拉選單

- M/T 排班頁以單一下拉選單列出該工作區已建立的 Demand 月份，選取既有月份後直接開啟，不再顯示額外的「開啟」按鈕。
- 下拉選單提供「新增月份…」；只有選取此項時才顯示月份輸入與「新增」按鈕，建立成功後回到該月份。

## 2026-08-14：Demand 改為可重建工作資料並使用分步介面

- Demand 是可反覆編輯、刪除及同月份重建的工作資料；刪除只移除 Demand 人員／日格及自行上傳的 previous schedule，不刪除既有求解紀錄、候選班表、人工修改班表或採用班表。
- M/T 排班頁改為 wizard：月份、上月班表、本月需求、M 萬年班表（僅 M）、求解。同一時間只顯示目前步驟，避免不同用途的 CSV 上傳與大型 Demand 表格同時堆疊。

## 2026-08-14：Demand 承接上月累計並改為就地編輯

- 建立 Demand 採用資料庫內上月 `★`，或使用者上傳 previous schedule 時，系統依員工 ID 將上月月底區間累計 R/R1 與萬年班表代號自動帶入本月；帶入後仍是可修改的 Demand 工作資料。
- 月初 R/R1、M 上月萬年班表與其他月份欄位直接在寬表內修改並於變更完成後自動儲存，不再透過每列「編輯」按鈕。
- Web 日格快捷選單只提供空白、R*、早、午／小、夜、其他 X 與共同設定非常態班型；其他 X／非常態班型在選單下段分組呈現。CSV 的既有完整合法值契約不變。

## 2026-08-14：班表明細讀取、SQLite 併發與失效登入狀態

## 2026-08-18：X 輸出保留班型註記

- 月班表日格新增向下相容格式 `X[HH:mm-HH:mm|註記]`；舊格式 `X[HH:mm-HH:mm]` 仍可讀取。
- `註記` 來源沿用既有 `EventDescription`，可保存非常態班型名稱、代碼或人工輸入說明；去除前後空白後必須為 1–500 字。
- 日格若填非常態班型名稱或代碼，解析成 X 時同步把 `班型` 欄或 `代碼` 寫入註記；純 `X[時間]` 不依時間自動反查註記，避免同時間多班型歧義。
- 候選、歷史與下載 CSV 不再一律剝除 X 註記；有註記時輸出 `X[HH:mm-HH:mm|註記]`，無註記時仍輸出 `X[HH:mm-HH:mm]`。
- X 註記只屬輸出與顯示元資料，不影響既有 solver 規則、固定格判定、休息時間或任何目標函數。

- 完整班表與驗證會同時讀取人員日格、外派、休假區間及相關設定；這些 EF Core 多集合查詢改用 split query，避免 M 班表在單一 SQL 形成集合笛卡兒積。
- 開發用 SQLite 在 migration 後切換為 WAL journal，使背景求解保存整份班表時，UI 班表讀取不再以預設 delete journal 互相阻塞。SQL Server 行為不變。
- Blazor 互動中的驗證狀態失效時，版面強制重新載入根頁；後續由既有 Cookie middleware 將未登入要求導回登入首頁，避免失效頁面看似仍可操作但沒有反應。

## 2026-08-14：Web 寬表完全沿用 CLI 月表，候選沿用求解時上月班表

> 寬表「視覺複製 CLI 46 欄」已由同日稍後的「建立班表、需求編輯器與班表編輯器 UI 契約」取代。候選跨月驗證改讀 `SourceRun` 快照的部分仍有效。

- Demand 與候選班表的寬表不再自行選欄、改順序或另算顯示內容；兩者直接沿用 `ScheduleCsv` 的 46 欄表頭與 row formatter。短月份仍顯示 1–31 並在不存在日期留空，M/T 不適用欄位也留空。
- 日格顯示沿用 CLI 正規化輸出，包括 M 完整站碼、M 小班名稱、中括號指定休假及 X 起訖時間。刪除 CLI 月表沒有的右側 Web 統計欄與底部 Coverage／品質列，驗證問題保留在警示清單。
- 候選班表有 `SourceRun` 時，十一小時、連續七日等跨月驗證固定讀取該次求解 input snapshot 的 previous schedule。這份 previous schedule 可以來自上月 `★`，也可以是使用者直接上傳；只有沒有 source run 的版本才退回查詢資料庫內上月 `★`。

## 2026-08-14：上月班表問題只標記警告

> 其中「上傳後建立 Imported 版本並可採用」已由 2026-08-17「只作 Demand 歷史快照」取代。對上傳內容做規則檢查、結果只標警告、不阻擋後續求解，仍然有效。

- 建立班表流程上傳的上月班表仍執行班表規則與人力問題檢查，但所有檢查結果只標記警告，不阻擋採用。
- 上傳內容保存為 `Imported` 歷史班表版本，供檢視、編輯、採用與本月求解使用。

## 2026-08-14：Demand 上傳的上月班表可成為正式版本

> 已由 2026-08-17「建立班表上傳的上月班表只作 Demand 歷史快照」取代。M 班表列表的獨立歷史匯入仍會建立 `Imported` 版本。

- 使用者在 Demand「上月班表」步驟直接上傳 previous schedule 時，typed JSON 仍作為該 Demand 的求解輸入；同一 transaction 另建立一份上月 `ScheduleVersion`，頁面提供「開啟版本」與「標示 ★」。
- 上傳不自動標星。直接上傳的版本沿用既有 `AdoptAsync`、同月唯一 `AdoptedSchedule`、紅色錯誤阻擋及 AuditLog，不新增第二套採用狀態。

## 2026-08-14：前端不顯示 M Coverage 與 R休 指定日期檢查

- 班表編輯器的「檢查結果」不產生 `M 班位 Coverage` 或 `R休 指定日期` 訊息。
- M Coverage 仍保留在班表底部的需求、內部安排與外派統計；solver 的 M 班位限制與 R休 指定日期限制不變。

## 2026-08-14：Web 排班工作 Dashboard 與可調求解參數

- Web UI 面向使用者一律稱「排班資料」，不顯示內部實體名稱 Demand；月份選單採短寬度，避免壓縮寬表日期欄。
- 首頁作為 Dashboard，集中顯示所有 `Queued`／`Running` 的求解工作；背景 worker 在排隊、開始與完成時通知 Blazor 元件，再沿用 Interactive Server 既有 WebSocket 推播畫面，頁面不以手動重新整理作為主要狀態來源，也不建立伺服器連回自身的 HTTPS 連線。
- M/T 工作區採兩層導覽：頂層僅 M、T、共同設定，進入 M/T 後才顯示該工作區頁面。M 萬年班表上傳移至上月班表步驟。
- Web 求解可設定每 seed Time Limit、worker 數與 M 的 seed 數；seed 值由伺服器在排程時產生並保存。T 固定一個 seed。Time Limit 若已有合法候選，仍保存候選並在 UI 明確標為可用，不視為失敗。
- 直接上傳版本沒有上上月正式歷史時，跨月驗證不得把未知日期視為工作：十一小時只比較已知工作，連續七日一般 R 從第一個資料完整的七日視窗開始；若已有上上月 `★`，仍做完整跨月驗證。

## 2026-08-14：M 萬年班表顯示上傳紀錄

- M 八週萬年班表與上月班表相同，於上傳區塊顯示檔名、上傳時間及開啟／下載操作；萬年班表仍是 Demand 的求解提示資料，不建立 `ScheduleVersion`。

## 2026-08-14：建立班表、需求編輯器與班表編輯器 UI 契約

- 「建立班表」是月份、上月班表、M 萬年班表、需求編輯器與求解的完整流程；「需求編輯器」只指 `demand.csv` 寬表；求解或上傳完成後一律是可由同一班表編輯器自由修改的一般班表。
- Web 寬表不再視覺複製 CLI 46 欄。需求編輯器只顯示輸入；班表編輯器顯示緊湊日格、56 日區間分界與累計、本月統計，M 底部另顯示各站每日人數及外派。
- Web 與新 CSV 輸出使用 `R*`、`R1*`、`R休*` 表示已決定的指定休假；讀取端繼續接受中括號舊格式。需求編輯器中的 `R*` 仍表示尚未決定的指定休假。
- 班表日格不用大面積班別底色：週末與國定假日整欄淺黃；休假深紅粗體；早班一般黑字；M 小班／T 午班黑字加底線；夜班粗體；X 顯示名稱或代碼並使用藍字淺藍底。
- 紅色只表示不足十一小時與連續七日沒有一般 R。其他 hard rule 與 Coverage 仍阻擋採用，但不冒充勞基法紅色。UI 使用「採用」「已採用」，不使用「標星」或單獨星號表示操作。
- Dashboard 顯示最近 5 筆而非僅 active jobs；成功及已有合法候選的 Time Limit 均顯示已解出，並顯示與 CLI 相同的目標分數。

## 2026-08-17：M 班表列表管理歷史班表與全域萬年班表

- M 班表列表可指定歷史月份後上傳月班表；匯入後建立可編輯的 `Imported` 版本，不覆蓋同月版本、不自動採用，沿用既有開啟、下載、採用及封存流程。
- M 只保存一份不綁月份的全域 56 日萬年班表。重新上傳直接覆蓋全域內容，不保留多版本；班表列表顯示檔名、最後修改時間、預覽、下載及可編輯寬表。
- 萬年班表寬表每列為一個模板代號，允許新增、刪除、改名與編輯第 1–56 日。日格接受空白、`R` 或合法 M 站早／小／夜班；尾端唯讀統計各班別數量，不區分車站，空白與 `R` 不計數。
- M Demand 沒有暫用模板時，求解採用當下的全域模板；Wizard 上傳只保存為該 Demand 的暫用模板，不覆蓋全域內容，並可清除以改回全域模板。全域與暫用模板皆不存在時仍可求解。
- ScheduleRun 排入佇列時保存當下實際採用的模板快照；之後覆蓋或編輯全域模板，不回溯改變既有求解工作或班表。

## 2026-08-17：建立班表可選擇資料庫內的上月版本

- 上月班表步驟列出同工作區、前一月份、尚未封存的所有資料庫班表版本；Editor 可從下拉選單指定其中一份作為本月求解歷史，選取不會自動採用該版本。
- 選取資料庫版本時，依員工 ID 更新本月 Demand 的月初 R/R1 與 M 萬年班表代號；求解排隊時仍保存實際使用的上月班表快照。
- M 萬年班表區塊的上傳與操作列對齊上月班表樣式；上傳按鈕使用相同主色，CSV 新分頁操作統一稱為「開啟班表」。

## 2026-08-17：建立班表上傳的上月班表只作 Demand 歷史快照

- 上月班表來源由單一下拉選單決定，資料庫版本與 `（採用上傳班表）` 是互斥來源；有上月 `★` 時預設使用該版本，沒有上月 `★` 時預設切到上傳來源。
- 建立班表精靈上傳的 previous schedule 僅保存於該 Demand 的解析快照，供本月求解使用；不建立或顯示為上月 `ScheduleVersion`，也不進入版本採用或編輯流程。
- 只有選到 `（採用上傳班表）` 時才顯示下載範本、上傳、檔名與上傳時間；資料庫版本與上傳快照都提供唯讀寬表預覽與 CSV 下載。
- M 班表列表的獨立歷史班表匯入維持原行為，仍會建立 `Imported` 版本。
- CSV 匯入忽略完全空白列，以及每列最後多出的空白欄；中間欄位或含資料的額外欄位仍依原格式驗證。

## 2026-08-17：T 要求 R1 要盡量在當月休完

- 假設該月分到兩個 R1，solver 應盡量在該月休完；此為 T 的 J3 軟目標，不是硬限制。

## 2026-08-17：M 需求跨三站群組固定班視為人工支援

- M Demand 的固定正常班可指定任一 LB 站；不在員工所屬三站群組時，仍保留正常工作，視為人工指定跨群組支援並計入指定站點 Coverage。
- Solver 僅為該固定日期建立跨群組站點變數；所有未固定日格仍只建立所屬三站群組變數，不能由 Solver 自動跨群組排班。

## 2026-08-17：M 排班品質與公平性權重調整

- `ExternalStaffing` 的實際權重由 100 改為 5；違反量定義與原三站 70 人次、LB09 4 人次的免罰門檻不變。
- `WorkStreak` 由 40 改為 20，`MixedShiftWorkStreak` 由 30 改為 15；兩者違反量定義不變。
- `EarlyAfternoonImbalance` 由 2 改為 20；早班與小班差超過 4 的違反量定義不變。

## 2026-08-17：M 全域萬年班表使用獨立分頁

- M 工作區新增「萬年班表」分頁，承載全域 56 日萬年班表的上傳、預覽、下載與寬表編輯。
- 班表列表不再顯示萬年班表，只管理歷史匯入與一般班表版本。

## 2026-08-17：班表可取消採用與改名

- 取消採用只移除該工作區與月份的 `AdoptedSchedule` 指標，不刪除班表版本、內容或稽核紀錄；該月份可暫時沒有採用班表。
- 未封存班表可修改名稱；名稱不要求唯一，且不改變班表內容、採用狀態或求解歷史。

## 2026-08-18：Web 匯入 demand CSV 時萬年班表留白承接上月

- 既有規則：本月留白承接上月同員工代號，有值覆蓋。
- Web `ImportDemandAsync` 先前整列取代員工，空白或舊版無此欄會把已從上月帶入的代號清成 null。
- 改為匯入後，M 員工若 CSV 萬年班表留白，從目前選定的上月班表補代號；CSV 有值仍覆蓋。求解器 hint 仍只讀本月欄（本月空白才在 solver 內再繼承上月）。

## 2026-08-18：AuditLog 登入工作階段與人工可讀呈現

- 每次登入成功產生新的 `SessionId`，寫入身分 Cookie 的 `audit_session_id` Claim；後續異動、下載與登出均帶同一 SessionId，便於追查疑似冒用帳號的修改軌跡。
- 登入失敗仍記錄輸入帳號、IP、User-Agent 與 request correlation ID，但不建立 SessionId。
- AuditLog 新增可篩選的 `SessionId` 欄；`ActorUserId` 與帳號快照一律取自已驗證 principal，禁止由前端或 service 呼叫端任意傳入。
- 異動寫入時快照員工代碼／姓名、日期、月份、班表名稱等上下文至 before／after JSON；UI 由集中呈現器產生繁中動作、操作對象摘要與欄位前後差異，不另存重複 Summary 欄。
- 舊 AuditLog 不做相容或回填；重建資料庫即可。
- 若他人已取得本人密碼或裝置，應用程式 Log 只能提供帳號、SessionId、IP 與時間等跡證，無法單獨證明鍵盤前真實人物；MFA 或公司 SSO／AD 不在第一版自行發明。

## 2026-08-18：員工需求填報與 Demand 一次性匯入

- M、T 各有獨立「員工需求填報」頁；任何已登入者可選任一員工填寫 R*、當月指定 R休 上限、固定正常班與 X。帳號與員工主檔不綁定；代填時 AuditLog 保存操作者、被填員工與 before／after。
- 同一工作區／月份／員工只保留一份目前填報；覆蓋採 revision token。任何人選取已有填報的員工時可讀取並覆蓋。
- 填報與正式 Demand 分離。Editor 在 Demand 需求寬表按「匯入員工填報」，先預覽再確認；正式 Demand 須已存在，每月份 Demand 只能匯入一次，覆蓋對應員工的 R休 上限與日格，不動月初 R/R1、T 月班別、萬年班表與員工主檔欄位。
- 匯入完成後記錄截止時間；之後填報仍可保存，UI 標示晚於本次匯入且不修改 Demand。員工主檔、求解與採用仍只限 Editor。

## 2026-08-18：M CSV 正常班以站號簡寫輸出

- M 月班表、萬年班表與班表版本 CSV 下載時，正常班日格一律輸出 `1早`、`12小` 等站號簡寫，不再輸出 `LB01早` 等完整站碼。
- 匯入仍接受 `LB01早` 與 `1早` 兩種格式；讀入後內部仍正規化為 `LB01` 等站碼。

## 2026-08-18：T 能力值限 T Editor 檢視

- 個別 T 人員能力值只對 T Editor 與 Administrator 顯示；Viewer 與只有 M 編輯權的帳號從員工主檔、既有 Demand／班表快照等讀取路徑都不得取得該值。
- T 能力仍是員工匯入、需求快照、求解與驗證所需的內部資料，不改變既有能力規則。
- 所有班表 CSV 下載均移除「能力」欄，不因下載者權限而例外；供 T Editor 匯入的員工與歷史班表範本仍保留能力欄。

## 2026-08-18：建立班表區依工作區編輯權隱藏

- 「求解新班表」導覽與完整建立班表頁只開放該工作區 Editor 與 Administrator。
- M Editor 在 T 工作區、T Editor 在 M 工作區及純 Viewer 均看不到入口，直接開啟網址或讀取 Demand／該月份求解工作資料也必須拒絕。
- 其他 M/T 員工資料與既有班表維持所有登入者可檢視；此決策只限建立班表區，補充 2026-08-13 的工作區授權決策。

## 2026-08-18：第一個穩定版 v1.0.0

- 以 git annotated tag `v1.0.0` 標示目前 `main` 為第一個穩定快照。產品範圍仍是文件所稱「第一版」（站務 M、檢修 T），此 tag 是該範圍的第一個穩定程式版本，不是範圍擴充。
- 組件 `Version`／`InformationalVersion` 為 `1.0.0`；Web 頁首顯示此號，`ScheduleRun.ProgramVersion` 寫入同一字串。
- 本快照不包含：正式 Linux＋SQL Server 部署環境驗收、上線前密碼政策強化、行控 S，以及文件已列為不納入第一版的項目。

## 2026-08-18：規格文件改依閱讀順序整理

- `docs/10-decisions.md` 改為由舊到新排列；新決策追加於文末。被取代的決策留在原日，並加註後列決策。
- `docs/08-frontend.md` 改依導覽、填報、建立班表、編輯器、採用、Log 書寫，不再把畫面契約堆在「導覽」一節。
- 已完成且會與現行規格衝突的 `docs/superpowers/` 實作草稿刪除；產品規格以 `01`–`11` 為準。

## 2026-08-18：數學模型報告移入 docs

- `tex/` 整目錄改為 `docs/tex/`。D-21 指定的唯一數學模型真相來源改為 `docs/tex/main2.tex`；編譯產物與 LaTeX ignore 一併搬移。
- 舊決策中的 `tex/main2.tex` 路徑視為搬移前記錄，以後列路徑為準。

## 2026-08-18：M/T 早午夜起訖時間改為可維護設定

- 早、午、夜班起訖時間改存入 `ConfigurationRevision`，不再硬編碼於 Solver 與驗證原始碼。M 與 T 各有獨立的 `WorkspaceShiftTimes`（Early/Afternoon/Night 各一對 `TimeOnly`）。
- 預設值維持現行：M 06:30–14:30、14:20–22:20、22:00–翌日 07:00；T 07:00–15:00、15:00–23:00、23:00–翌日 07:00。既有 `ConfigurationRevision` 回填此預設值，既有班表驗證結果不變。
- 驗證規則：起訖不可相同；結束早於開始視為跨日；三班時間窗可互相重疊（與 CH03 實際排班限制無關）。
- 工作區班別時間由對應工作區 Editor 維護（`RequireEditor(actor, workspace)`）；共同設定頁不開放此項。
- 設定變更只影響之後新建的 Demand、求解與匯入班表：新建 Demand 綁定當下 `ConfigurationRevisionId`，求解時將班別時間寫入 `InputSnapshotJson`，既有資料沿用原 revision 或 fallback 常數。
- Solver 與人工驗證共用同一解析邏輯：`ScheduleInput.StandardShiftTimes` 有值則用，否則 fallback 現行常數，確保舊快照重跑結果一致。
- M/T 各增「班別時間設定」第二層頁（`/m/shift-times`、`/t/shift-times`）；Viewer 唯讀。

## 2026-08-18：工作區別月班表下載欄位

- M 班表、需求與上月範例／下載 CSV 移除「能力」與「T月班別」；保留「萬年班表」。
- T 班表、需求與上月範例／下載 CSV 移除「萬年班表」；保留「能力」與「T月班別」。
- 內部匯入仍使用完整 46 欄；此決策只影響 Web 下載與範例表頭，取代「2026-08-18：T 能力值限 T Editor 檢視」中「所有班表 CSV 下載均移除能力欄」的表述。

## 2026-08-18：穩定版 v1.0.1

- 以 git annotated tag `v1.0.1` 標示目前 `main` 為第一版範圍內的修訂穩定版。
- 組件 `Version`／`InformationalVersion` 為 `1.0.1`；Web 頁首與 `ScheduleRun.ProgramVersion` 沿用同一字串。

## 2026-08-18：需求編輯器還原上月 R/R1 與萬年班表

- 需求編輯器（步驟三）新增「還原上月 R/R1」按鈕；M 另含萬年班表代號。
- 依步驟一已設定的上月班表來源（採用版或上傳快照）重新帶入 `月初區間累計R`／`R1` 與 M `萬年班表`，邏輯同建立 Demand 或切換上月來源時的繼承，不影響其他欄位與日格。
- 僅在已有上月班表來源時顯示；無來源時後端回報「找不到上月班表來源」。

## 2026-08-18：員工填報可重複匯入 Demand

- 取代「2026-08-18：員工需求填報與 Demand 一次性匯入」中「每月份 Demand 只能匯入一次」與「完成後顯示時間與操作者」的 UI 行為。
- 需求編輯器「匯入員工填報」按鈕在匯入後維持可用；Editor 可再次預覽並確認，覆蓋對應員工的 R休 上限與日格。
- `DemandSubmissionImport` 改為記錄最近一次匯入時間；重複匯入時更新此紀錄，填報頁仍以之判斷晚於截止的填報。

## 2026-08-18：修改還原上月 R/R1 與萬年班表為統計上月 R/R1 與萬年班表
- 修改文字敘述

## 2026-08-19：登入導向只接受站內位址

- 登入與改密碼完成後的導向位址一律相對於應用程式根位址解析，解析結果不在根位址之下者改導向首頁。
- 涵蓋 `//host`、`https://host`、`\\host` 與 `javascript:` 等形式的 `ReturnUrl`，避免登入成功後被導向外部網站（open redirect）。
- 站內位址行為不變：`RedirectToLogin` 產生的相對路徑（無前導斜線）與帶前導斜線的路徑都照原樣返回，查詢字串保留。

## 2026-08-19：求解工作可手動取消

- `ScheduleRunStatus` 新增終態 `Cancelled`；狀態以字串保存，不需要 migration。
- 等待中（`Queued`）與求解中（`Running`）的工作都可取消，權限與送出求解相同：該工作區 Editor 或 Administrator。
- 取消一律丟棄求解中途已找到的候選，不寫入任何 `ScheduleVersion`；若需要保留中途結果，屬另一項「提前結束並取用目前最佳解」功能，本決策不涵蓋。
- 取消要求記為 AuditLog `ScheduleRunCancelled`（記錄取消者與取消當下狀態）；終態 `Cancelled` 由背景工作在求解實際停止後寫入並推播，因此按下取消到狀態更新之間有短暫延遲。
- 取消入口同時放在建立班表頁的「最近求解工作」與首頁 Dashboard；Dashboard 逐列依該筆所屬工作區的編輯權限決定是否顯示按鈕，顯示範圍不變。
- 取消訊號只存在於單一程序記憶體。系統重啟時未完成的工作依既有規則重新排隊，先前的取消要求不會保留。

## 2026-08-19：M 多 seed 改為依序執行並限制求解參數

- 取代「2026-08-12：M CLI 新增 `--m-search`」與「2026-08-12：候選時間改以餘時分配」中「並行 seed 數」與「並行使用量約為 `workers × seeds`」的敘述。
- M 的多 seed portfolio 由並行改為**依序執行**，Web 背景工作與 CLI 一致。每個 seed 仍各自取得完整的 worker 數與完整時限，因此總耗時約為 `seeds × seconds`（例如 300 秒 × 2 seeds 約 600 秒）。
- 依序執行後，同時只有一次求解在記憶體中，資源用量不再隨 seed 數相乘；`workers × seeds` 不再是資源上限的判斷依據，改以總佔用時間為準。
- Web 求解參數上限：每 seed 時限 600 秒、worker 數 8、seed 數 4（T 仍固定 1 seed）。超出上限由 `ScheduleRunService.QueueAsync` 擋下，上限常數定義在 `ScheduleRunOptions`，前端輸入欄同步套用 `max`。
- Web 預設值改為 300 秒、8 workers、1 seed（M/T 相同）；CLI 省略 `--search` 時改為 8 workers、2 seeds、300 秒。
- 選取規則不變：比較各 seed 第一候選的具名目標字典序分數，只採用較佳 seed 的整批候選。

## 2026-08-19：穩定版 v1.0.2

- 以 git annotated tag `v1.0.2` 標示目前 `main` 為第一版範圍內的修訂穩定版。
- 組件 `Version`／`InformationalVersion` 為 `1.0.2`；Web 頁首與 `ScheduleRun.ProgramVersion` 沿用同一字串。
- 本版新增 `docs/12-deployment.md`（Ubuntu 24.04＋SQL Server 部署指南），程式行為未變更。
- 上線前密碼政策強化與部署環境驗收仍未完成，範圍與 v1.0.1 相同。

## 2026-08-20：已登入者可自行修改密碼

- 既有 `/Account/ChangePassword` 原本只在首次登入或管理者重設後強制導向；頁首新增「修改密碼」，任何已登入者可隨時開啟同一頁。
- 自行修改仍須驗證目前密碼，寫入 AuditLog `PasswordChanged`，並保留同一 SessionId；不開放忘記密碼或郵件重設。
- 首次登入／重設後強制改密碼的攔截不變，該狀態下不可返回其他頁面。

## 2026-08-20：SQLite 與 SQL Server 使用獨立 migration sets

- 取代「單一 provider-neutral migration」設計；SQLite 與 SQL Server 分別使用 `NtmcScheduler.Migrations.Sqlite` 與 `NtmcScheduler.Migrations.SqlServer` assembly，避免 provider metadata 差異造成 pending model changes。
- 既有 migration 歷史與 snapshot 原樣移至 SQLite project；SQL Server 以目前完整 model 的 `InitialCreate` 起始，適用於尚無 NtmcScheduler schema 的正式資料庫。
- 每次 model 變更都必須對兩個 provider 各新增 migration；Web runtime 與 design-time factory 依 provider 指定對應 migration assembly，不停用 `PendingModelChangesWarning`。

## 2026-08-20：Blazor Server 以 IDbContextFactory 避免共用 NtmcDbContext

- 取代 Blazor Interactive Server circuit 內長期注入 scoped `NtmcDbContext` 的用法。Circuit scope 存活期間長，且 `WorkspaceNav` 與頁面元件可同時查詢，會觸發 `A second operation was started on this context instance` 與 `Cannot access a disposed context instance`。
- Application service 與 `CurrentActorService` 改為注入 `IDbContextFactory<NtmcDbContext>`，每個操作 `CreateDbContextAsync()` 後 `await using` 釋放。班表驗證若在同一交易內執行，由 `ScheduleService` 把同一個 context 傳入 `ScheduleValidationService`，避免未提交變更對另一個 context 不可見。
- 啟動 `MigrateAsync`、HTTP 下載端點、登入／改密碼靜態表單，以及 `ScheduleRunWorker` 自行建立的短生命週期 scope 仍直接解析 `NtmcDbContext`。`AddEntityFrameworkStores<NtmcDbContext>()` 仍需要 scoped context。
- `UserAdministrationService` 因 Identity `UserManager` 必須與 EF 共用同一 context 與交易，改為每個操作建立短生命週期 scope，不在 circuit 上長期持有 context。
- 不用 lock／`SemaphoreSlim` 把共用 context 排隊。

## 2026-08-21：M Demand 可選擇不使用萬年班表

- M 建立班表的萬年班表步驟新增「不使用萬年班表」選項。它為該月份保存空萬年班表，明確覆蓋全域模板，求解時不加入任何萬年班表 hint。
- 這個選項不刪除或修改全域模板，也可隨時改回全域模板或上傳本月份暫用模板。

## 2026-08-21：月份排班參數與每次求解權重前端化

- M 車站固定為 `LB01`–`LB12`，不可新增、刪除或改名；群組、早／小／夜班人數上下限與外援等級保存於 Demand 月份快照。新月份複製前一個日曆月，沒有前月時使用既有 12 站行為。群組不再固定三站。
- 外援分為不允許、盡量不要、允許；允許車站共用 70 人次免罰額度，盡量不要從第一人次計分。舊 LB02／LB04／LB11 為允許、LB09 為盡量不要，其餘不允許；本決策取代 LB09 前 4 人次免罰。
- M/T 每月 R／R1 基準可調但仍為軟目標；56 日精確額度、七日一般 R 與 11 小時休息等硬規則不變。R1 保留跨月累積餘額，跨兩區間按國定假日比例分配本月目標。
- 求解頁列出硬規則唯讀說明；目前啟用的軟規則權重可逐次求解調整，0 表示不建模。Priority、公式、70 人次額度及歷史停用規則不可調整。ScheduleRun 與 ScheduleVersion 保存實際設定快照。

## 2026-08-21：M 品質群組優先層級改為 2

- M 的 `ScheduleQualityAndFairness` 由 Priority 4 改為 Priority 2，使目前兩個目標群組依序為 Priority 1、2。
- 此變更只調整群組編號；既有 `J4+J5` 公式名稱、違反量、權重與字典序求解順序不變。T 仍維持 Priority 1–5。
- 已保存的 ScheduleRun／ScheduleVersion 快照不改寫，班表列表仍相容顯示其中原有的 Priority 4。

## 2026-08-21：穩定版 v1.1.0

- 以 git annotated tag `v1.1.0` 標示目前 `main` 為第一版範圍內的次版穩定版。
- 組件 `Version`／`InformationalVersion` 為 `1.1.0`；Web 頁首與 `ScheduleRun.ProgramVersion` 沿用同一字串。

## 2026-08-21：M 非所屬站指派恢復計分並給予個人額度

- M 的非所屬站指派（`NonHomeStation`）重新加入 `ScheduleQualityAndFairness`，預設權重為 1。
- 違反量改為逐人計算：每位人員目標月前 8 班非所屬站正常工作免罰，超過部分每班計 1，再加總所有人員。
- 合法站群組的硬限制不變；此決策取代先前停用非所屬站指派，以及以全部非所屬站工作人次直接計分的定義。

## 2026-08-21：穩定版 v1.1.1

- 以 git annotated tag `v1.1.1` 標示目前 `main` 為第一版範圍內的修訂穩定版。
- 組件 `Version`／`InformationalVersion` 為 `1.1.1`；Web 頁首與 `ScheduleRun.ProgramVersion` 沿用同一字串。
- 本版恢復 M 非所屬站指派（`NonHomeStation`）計分：每人前 8 班免罰，預設權重 1。

## 2026-08-21：新增環狀站務 YM 並與三鶯站務 M 共用 solver

- 既有工作區代碼 `M`、`T` 與所有正式資料保持不變；UI 名稱改為「三鶯站務 M」與「三鶯檢修 T」。新增獨立工作區 `YM`（路由 `/ym`），UI 名稱為「環狀站務 M」。所有登入者可檢視，YM 編輯權獨立授予，既有 M Editor 不自動取得 YM 權限。
- YM 固定站點為 `Y06`–`Y19`。沒有前月設定時，預設群組依序為 Y06–Y08、Y09–Y11、Y12–Y14、Y15、Y16–Y17、Y18–Y19；各站早／小／夜班皆為最少 1、最多 1，且禁止外援。有前月設定時只沿用同工作區前月快照。
- YM 正常班 CSV 與日格輸出 `6早`、`6小`、`6夜` 等簡寫；匯入同時接受簡寫與 `Y06早`、`Y06小`、`Y06夜`，內部保存完整站碼。
- M 與 YM 的硬規則、軟目標、Priority、權重、候選搜尋及驗證目前完全相同，因此共用同一個 `MSolver`，不複製 `YMSolver`。工作區層負責各自站碼、預設設定、班別時間、資料與權限隔離。若後續 M 與 YM 規則需要不同，必須另案更新規格、數學模型、決策、實作與測試，再選擇明確分支或拆分 solver。
- M 與 YM 各自保存全域萬年班表與班別時間。YM 班別時間初值使用 M 原始預設 06:30–14:30、14:20–22:20、22:00–翌日 07:00，之後獨立維護。
- workspace 資料庫欄位由 1 字元擴為 2 字元，但既有 `M`／`T` 值不搬移、不改寫。全域萬年班表新增 workspace 歸屬；既有資料列歸屬 `M`，YM 另建新列。SQLite 與 SQL Server 各自新增 migration，正式套用前必須以備份副本驗證既有資料筆數、主鍵、關聯與內容保持不變。
- 組件 `Version`／`InformationalVersion` 升為 `1.2.0`；Web 頁首與 `ScheduleRun.ProgramVersion` 沿用同一字串。

## 2026-08-21：新增環狀檢修 YT 並與三鶯檢修 T 共用 solver

- 既有 `T` 工作區及正式資料保持不變。新增獨立工作區 `YT`（路由 `/yt`），UI 名稱為「環狀檢修 T」。所有登入者可檢視，YT 編輯權獨立授予，既有 T Editor 不自動取得 YT 權限。
- YT 的員工欄位、專業分組、能力、每月固定班別、硬規則、軟目標、Priority、權重、候選搜尋與驗證目前完全等同 T，因此共用同一個 `TSolver`，不複製 `YTSolver`。若後續 T 與 YT 規則需要不同，必須另案更新規格、數學模型、決策、實作與測試，再選擇明確分支或拆分 solver。
- T 與 YT 的員工、需求、填報、求解工作、班表版本、採用版、權限與班別時間依 workspace 隔離；YT 班別時間初值使用 T 預設 07:00–15:00、15:00–23:00、23:00–翌日 07:00，之後獨立維護。
- v1.2.0 已將 workspace 欄位擴為 2 字元，因此新增 YT 不需要資料表結構 migration，也不改寫任何既有 M/T/YM 資料。組件 `Version`／`InformationalVersion` 升為 `1.3.0`。
